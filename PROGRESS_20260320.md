# Progress Summary — 2026-03-20

## Running Processes

| GPU | Process | Status |
|-----|---------|--------|
| 0,1 | DiT training (fixed manifest, from pretrained) | Step ~24.4K/60K (41%), loss ~0.49 |
| 2 | SLat refiner full training | Step ~10.5K/20K (53%), loss ~4.8 |
| 3 | Available (overfit2 completed) | — |

## Completed This Session

### 1. VAE Topology Analysis
- **Finding**: PartPacker VAE (64³ FlexiCubes) destroys CC topology: OBJ CC 8.34/5.37 → VAE 1.81/2.35
- **VAE finetune failed**: even with Blender Voxel Remesh for watertight data, finetune hurt pretrained quality
- **FlexiCubes resolution capped at 64**: 128/256 produce NaN (confirmed)
- **Conclusion**: VAE architecture is the bottleneck, not training data

### 2. TRELLIS 2 SLat Integration
- **Architecture**: DualPartSLatModel with part cross-attention + VJEPA projection (1603M total, 311M trainable)
- **Environment setup**: Critical fix — system CUDA 13.0 nvcc vs PyTorch CUDA 12.4 caused all crashes (hashmap, triton). Solved by conda nvcc 12.9
- **SLat VAE roundtrip**: Part mesh reconstruction far better than PartPacker VAE (隔板、按钮、曲面细节全保留)
- **Overfit test**: Loss 30→1.0 on 5 samples (5K steps), model can learn the task

### 3. Data Pipeline
- **SLat GT cache**: 4500/4636 part meshes encoded to SLat latent
- **Blender Voxel Remesh**: 2592 non-watertight PhysX meshes successfully remeshed (0 FAIL)
- **Preprocessing bug fixes**: trimesh BVH memory leak (2-50GB/worker), multiprocessing OOM

### 4. Scripts Created
| Script | Purpose |
|--------|---------|
| `dual_part_slat.py` | DualPartSLatModel architecture |
| `preprocess_slat_gt.py` | GT SLat encoding |
| `train_slat_refiner.py` | SLat refiner training |
| `infer_slat_refiner.py` | SLat refiner inference |
| `preprocess_vae_data.py` | VAE finetune preprocessing (with Blender remesh) |
| `finetune_vae.py` | VAE finetune training |
| `eval_topology.py` | Topology CC evaluation |
| `eval_vae_roundtrip.py` | VAE roundtrip evaluation |

### 5. Key Technical Findings
- **mesh.contains() leaks 2.5GB/mesh** via embree BVH cache → use scipy KDTree instead
- **multiprocessing + meshiki leaks memory** → maxtasksperchild=10
- **FlexGEMM H200 bug** (GitHub Issue #95) → confirmed, workaround: recompile with matching CUDA
- **spconv backend incompatible** with FlexGEMM weights (conv key naming difference + upsample behavior)

## Next Steps
1. Wait for full SLat training to finish (~2h)
2. Run inference with trained SLat refiner → generate refined meshes
3. Render GT vs VAE-only vs VAE+SLat comparison
4. Integrate into full pipeline: video → DiT → VAE → SLat → mesh
5. End-to-end inference test
